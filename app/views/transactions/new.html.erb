<!-- Define Transactions
    from_user = @user
    to_user = chosen user in form (can be @user)
    account = to_user.account
    balance = from_user.balance
    reference = String defined by from_user
    -->
    <% stylesheet_link_tag 'transactions_new' %>
    <div class="balance">Available Balance : <%= @account.balance %></div>
